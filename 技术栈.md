1. SpringBoot（后端核心）
基于SpringBoot搭建后端脚手架，完成统一结果封装、全局异常处理、跨域配置、拦截器配置，实现业务接口分层开发（Controller、Service、Dao、Entity），完成基础业务CRUD功能。

2. Vue（前端核心）
使用Vue搭建前端页面，完成路由配置、组件封装、axios接口请求、页面布局搭建，实现用户登录、权限展示、业务数据展示、表单提交、分页查询等前端交互功能，页面布局整洁、交互流畅。

3. MySQL（持久化数据库）
设计合理的数据库表结构，包含主表、关联表，设置主键、外键、索引、非空、默认值等约束，满足项目业务数据持久化存储，禁止单表完成全部业务。

4. Nacos（服务配置/注册中心）
将SpringBoot项目注册到Nacos，实现服务注册发现；将项目配置文件（数据库地址、端口、密钥等）托管至Nacos，实现配置动态刷新，替代本地硬编码配置。

5. Redis（缓存中间件）
实际落地缓存业务，例如：登录用户信息缓存、热门数据缓存、分页数据缓存、接口限流、验证码缓存等，实现接口响应速度优化，掌握缓存基本使用与过期配置。

6. Elasticsearch（全文检索）
完成业务数据ES索引创建、数据同步，实现关键词全文检索、模糊查询、高亮显示等功能，替代传统MySQL模糊查询，必须在项目中体现ES检索的实际使用场景。

