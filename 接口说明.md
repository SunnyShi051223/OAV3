# 企业OA考勤系统接口说明

## 认证接口

| 方法 | 地址 | 说明 | 权限 |
| --- | --- | --- | --- |
| POST | `/api/auth/login` | 登录，返回 token、用户、角色、权限 | 公开 |
| GET | `/api/auth/info` | 获取当前登录用户信息 | 登录 |
| POST | `/api/auth/logout` | 退出登录，token 写入 Redis 黑名单 | 登录 |

## 制度文档接口

| 方法 | 地址 | 说明 | 权限 |
| --- | --- | --- | --- |
| POST | `/api/documents` | 新增制度文档，并同步 ES 索引 | `document:add` |
| PUT | `/api/documents` | 修改制度文档，并同步 ES 索引 | `document:update` |
| DELETE | `/api/documents/{id}` | 删除制度文档，并删除 ES 索引 | `document:delete` |
| GET | `/api/documents/{id}` | 查询文档详情 | `document:query` |
| GET | `/api/documents` | 查询文档列表 | `document:query` |
| GET | `/api/documents/search?keyword=请假` | ES 全文检索，支持高亮 | `document:query` |
| POST | `/api/documents/index/rebuild` | 将 MySQL 文档全量同步到 ES | `document:index` |

## 人事基础接口

| 模块 | 地址前缀 | 说明 |
| --- | --- | --- |
| 用户 | `/api/users` | 员工录入、修改、删除、查询、改密 |
| 部门 | `/api/departments` | 部门创建、修改、删除、树形查询 |
| 职位 | `/api/positions` | 职位创建、修改、删除、按部门查询 |
| 菜单 | `/api/menus` | 菜单创建、修改、删除、树形查询 |

## 当前验证结果

- 登录：`admin / 123456` 可登录
- Redis：退出登录后同 token 访问 `/api/auth/info` 返回 401
- Nacos：后端成功读取 `attendance-system.yaml` 并注册服务
- Elasticsearch：`/api/documents/index/rebuild` 成功同步 2 条文档
- Elasticsearch：`/api/documents/search?keyword=请假` 成功返回高亮结果
