# 同事检索模块 — 交付报告

## 版本信息

| 项目 | 内容 |
|------|------|
| 模块名称 | 同事检索 (Colleague Search) |
| 交付日期 | 2026-07-20 |
| 分支 | main |
| 依赖模块 | 用户管理、部门管理、角色权限体系（已完成） |

---

## 1. 功能概述

在现有 OA 考勤系统上新增"同事检索"模块，支持按**姓名、工号、手机号**模糊搜索同事，并根据当前登录用户的角色自动限制数据可见范围。

### 1.1 核心能力

- 关键词模糊检索（姓名 / 工号 / 手机号）
- 基于角色的数据范围自动过滤
- 仅展示非敏感基本信息

### 1.2 权限矩阵

| 角色 | role_code | 可查看范围 |
|------|-----------|-----------|
| 管理员 | ADMIN | 全部员工 |
| 主管 | MANAGER | 本部门全部同事 + 其他部门主管 |
| 员工 | EMPLOYEE | 仅同部门同事 |

所有角色自动排除自身，不会在搜索结果中看到自己。

---

## 2. 架构设计

### 2.1 调用链路

```
ColleaguesView.vue          # 搜索界面
  └─ api/colleagues.js      # HTTP 请求封装
       └─ GET /api/colleagues/search?keyword=
            └─ ColleagueController.java    # @PreAuthorize("hasAuthority('colleague:query')")
                 └─ ColleagueServiceImpl.java
                      ├─ DataScopeService   # 角色判断（ADMIN / MANAGER / EMPLOYEE）
                      └─ SysUserMapper.searchColleagues()  # 数据库模糊查询
```

### 2.2 过滤逻辑（ColleagueServiceImpl.canViewColleague）

```
if 当前用户 is ADMIN      → 全部可见
if 当前用户 is MANAGER    → (同部门) OR (目标用户 role_code = 'MANAGER')
if 当前用户 is EMPLOYEE   → 同部门
```

过滤在 Java 层完成，数据库层只做关键词匹配，保证灵活性。

---

## 3. 文件清单

### 3.1 新增文件

| # | 文件路径 | 说明 |
|---|---------|------|
| 1 | `oa-attendance-system/.../vo/ColleagueVO.java` | 同事信息响应 VO |
| 2 | `oa-attendance-system/.../service/IColleagueService.java` | 服务接口 |
| 3 | `oa-attendance-system/.../service/impl/ColleagueServiceImpl.java` | 服务实现（含 RBAC 过滤） |
| 4 | `oa-attendance-system/.../controller/ColleagueController.java` | REST 控制器 |
| 5 | `oa-attendance-web/src/api/colleagues.js` | 前端 API 模块 |
| 6 | `oa-attendance-web/src/views/ColleaguesView.vue` | 搜索页面组件 |
| 7 | `db_migration_20260720_colleague_search.sql` | 数据库迁移脚本 |

### 3.2 修改文件

| # | 文件路径 | 修改内容 |
|---|---------|---------|
| 1 | `oa-attendance-system/.../entity/SysUser.java` | 新增 `roleCode` 瞬态字段 |
| 2 | `oa-attendance-system/.../mapper/SysUserMapper.java` | 新增 `searchColleagues` 查询方法 |
| 3 | `oa-attendance-web/src/router.js` | 新增 `/colleagues` 路由 |

---

## 4. ColleagueVO 字段说明

| 字段 | 类型 | 来源 | 说明 |
|------|------|------|------|
| userId | Long | sys_user | 用户 ID |
| employeeNo | String | sys_user | 工号 |
| realName | String | sys_user | 真实姓名 |
| avatar | String | sys_user | 头像 URL |
| gender | String | sys_user | 性别 |
| phone | String | sys_user | 手机号 |
| email | String | sys_user | 邮箱 |
| hireDate | LocalDate | sys_user | 入职日期 |
| deptId | Long | sys_user | 部门 ID |
| deptName | String | sys_department (JOIN) | 部门名称 |
| positionId | Long | sys_user | 职位 ID |
| positionName | String | sys_position (JOIN) | 职位名称 |
| roleName | String | sys_role (JOIN) | 角色名称 |

> **安全说明**：不返回 `password`、`username`（登录账号）、`birthDate`（生日）等敏感字段。

---

## 5. 数据库变更

### 5.1 新增权限

```sql
permission_code: colleague:query
permission_name: 同事查询
module: colleague
```

### 5.2 新增菜单

```sql
menu_path: /colleagues
menu_name: 同事检索
component: ColleaguesView
parent_id: 0（一级菜单）
sort_order: 3
```

### 5.3 角色分配

三个角色（ADMIN / MANAGER / EMPLOYEE）均分配了菜单和权限。数据范围的差异化控制在后端 `ColleagueServiceImpl` 中实现，不依赖前端或权限配置。

### 5.4 执行方式

```bash
mysql -u<user> -p <database> < db_migration_20260720_colleague_search.sql
```

脚本使用 `WHERE NOT EXISTS` / `INSERT IGNORE` 保证可重复执行。

---

## 6. API 接口

### GET /api/colleagues/search

| 项目 | 内容 |
|------|------|
| 权限 | `colleague:query` |
| 参数 | `keyword` (String, optional, default "") |
| 成功响应 | `{ code: 200, msg: "查询成功", data: [ColleagueVO, ...] }` |
| 空关键字 | 返回空列表（不执行数据库查询） |

**请求示例：**
```
GET /api/colleagues/search?keyword=张三
```

**响应示例：**
```json
{
  "code": 200,
  "msg": "查询成功",
  "data": [
    {
      "userId": 5,
      "employeeNo": "EMP005",
      "realName": "张三",
      "avatar": null,
      "gender": "男",
      "phone": "13800138000",
      "email": "zhangsan@example.com",
      "hireDate": "2024-03-01",
      "deptId": 4,
      "deptName": "后端组",
      "positionId": 8,
      "positionName": "Java开发",
      "roleName": "员工"
    }
  ]
}
```

---

## 7. 部署步骤

1. **数据库迁移** — 执行 `db_migration_20260720_colleague_search.sql`
2. **后端构建** — `cd oa-attendance-system && mvn clean package -DskipTests`
3. **后端部署** — 替换 jar 包并重启服务
4. **前端构建** — `cd oa-attendance-web && npm run build`
5. **前端部署** — 将 dist/ 部署到 Web 服务器
6. **验证** — 分别使用 ADMIN / MANAGER / EMPLOYEE 账号登录，访问"同事检索"菜单

---

## 8. 测试建议

### 8.1 功能测试

| 场景 | 操作 | 预期 |
|------|------|------|
| 空搜索 | 点击检索（不输入关键词） | 返回空列表 |
| 按姓名搜索 | 输入完整姓名 | 返回匹配的同部门同事 |
| 按工号搜索 | 输入部分工号 | 返回匹配的可见同事 |
| 按手机号搜索 | 输入手机号片段 | 返回匹配的可见同事 |
| 自身排除 | 搜索自己的姓名 | 结果中不包含自己 |

### 8.2 权限测试

| 角色 | 操作 | 预期 |
|------|------|------|
| EMPLOYEE（后端组） | 搜索前端组同事 | 不可见，返回空 |
| EMPLOYEE（后端组） | 搜索本组同事 | 可见 |
| MANAGER（后端组） | 搜索前端组主管 | 可见 |
| MANAGER（后端组） | 搜索前端组普通员工 | 不可见 |
| MANAGER | 搜索本部门全部同事 | 全部可见 |
| ADMIN | 搜索任意部门同事 | 全部可见 |

### 8.3 边界测试

| 场景 | 预期 |
|------|------|
| 禁用账号 (status=0) | 不出现在结果中 |
| 已删除账号 (deleted=1) | 不出现在结果中 |
| 无 colleague:query 权限 | 菜单不可见；直接访问URL后端返回403 |

---

## 9. 后续迭代建议

1. **分页支持** — 当用户量大时，搜索结果应考虑分页
2. **高级筛选** — 按部门、职位、入职时间范围等组合筛选
3. **组织架构树** — 结合部门树以树形结构浏览同事
4. **在线状态** — 展示同事当前在线/离线状态
5. **收藏/常用联系人** — 支持标记常用联系人快速访问
6. **导出功能** — 支持导出可见范围内的同事信息为 Excel
