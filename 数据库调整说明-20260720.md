# 数据库调整说明 - 2026-07-20

本次调整围绕后续 Flowable 审批、统一申请管理、数据字典和制度文档标签检索展开。

## 1. 数据字典

新增：

- `sys_dict_type`：字典类型表
- `sys_dict_data`：字典数据表

默认已插入：

- `application_type`：申请类型
- `application_status`：申请状态
- `attendance_status`：考勤状态
- `user_status`：用户状态
- `document_tag`：文档标签

后续前端下拉、后端校验、状态展示应优先从字典读取，减少硬编码。

## 2. 审批和申请表

已删除旧表：

- `app_approval`
- `app_leave`
- `app_makeup`
- `app_overtime`

保留并重建：

- `app_application`

新的 `app_application` 是统一申请表，支持请假、加班、补卡和后续更多申请类型。审批流转交给 Flowable，业务表只保存业务单据和 `process_instance_id`。

核心字段：

- `application_id`：申请ID
- `applicant_id`：申请人ID
- `applicant_name`：申请人姓名
- `application_type`：申请类型
- `start_time`：开始时间
- `end_time`：结束时间
- `reason`：申请原因
- `attachment_urls`：申请附件URL列表，JSON
- `remark`：申请备注
- `status`：业务状态
- `process_instance_id`：Flowable 流程实例ID

## 3. 制度文档标签

新增标准标签模型：

- `doc_tag`：制度文档标签表
- `doc_document_tag`：制度文档和标签关联表

默认标签：

- 考勤
- 请假
- 补卡
- 加班
- 审批

后续开发文档检索时，MySQL 查询和 Elasticsearch 索引都应把标签纳入检索范围。

## 4. 执行脚本

本次迁移脚本：

```powershell
D:\HuaweiMoveData\Users\32874\Desktop\OAV3\db_migration_20260720_standard_application_dict_doc_tag.sql
```

执行命令：

```powershell
mysql -uroot -pshisannian1223 oa_system < D:\HuaweiMoveData\Users\32874\Desktop\OAV3\db_migration_20260720_standard_application_dict_doc_tag.sql
```
