# 考勤模块数据库设计说明

本说明对应迁移脚本：

```powershell
D:\HuaweiMoveData\Users\32874\Desktop\OAV3\db_migration_20260720_attendance_schema.sql
```

## 1. 设计目标

考勤模块需要支持：

- 管理员/主管创建考勤规则
- 员工签到、签退
- WiFi和定位双重校验
- 正常、迟到、早退、缺勤、请假、加班等状态
- 员工个人日历和月度统计
- 管理员/主管查看全局或部门统计

## 2. 表结构

### att_rule

考勤规则主表。

核心字段：

- `dept_id`：部门ID，空表示全公司通用
- `effective_start_date` / `effective_end_date`：规则生效期
- `work_start_time` / `work_end_time`：标准上下班时间
- `check_in_start_time` / `check_in_end_time`：允许签到窗口
- `check_out_start_time` / `check_out_end_time`：允许签退窗口
- `late_threshold`：迟到阈值
- `early_threshold`：早退阈值
- `overtime_threshold`：加班判定阈值
- `work_days`：工作日配置，JSON，例如 `[1,2,3,4,5]`
- `require_wifi`：是否要求WiFi校验
- `require_location`：是否要求定位校验
- `created_by`：创建人

### att_rule_wifi

规则WiFi表。一个考勤规则可以绑定多个WiFi。

核心字段：

- `rule_id`
- `wifi_ssid`
- `wifi_bssid`
- `enabled`

### att_rule_location

规则地点表。一个考勤规则可以绑定多个办公地点。

核心字段：

- `rule_id`
- `location_name`
- `latitude`
- `longitude`
- `radius`
- `enabled`

### att_record

考勤记录表。每个员工每天一条记录。

核心字段：

- `user_id`
- `dept_id`
- `rule_id`
- `rule_name`
- `attendance_date`
- `check_in_time`
- `check_out_time`
- `check_in_status`
- `check_out_status`
- `attendance_status`
- `check_in_wifi_ssid`
- `check_in_wifi_bssid`
- `check_in_latitude`
- `check_in_longitude`
- `check_in_distance`
- `check_in_valid`
- `check_in_fail_reason`
- `check_out_wifi_ssid`
- `check_out_wifi_bssid`
- `check_out_latitude`
- `check_out_longitude`
- `check_out_distance`
- `check_out_valid`
- `check_out_fail_reason`
- `work_minutes`
- `overtime_minutes`

唯一约束：

```sql
uk_record_user_date(user_id, attendance_date)
```

### att_check_log

打卡尝试日志表。成功和失败的签到/签退尝试都应记录。

用途：

- 排查员工为什么无法签到
- 留存WiFi和定位校验结果
- 记录失败原因

核心字段：

- `user_id`
- `rule_id`
- `record_id`
- `attendance_date`
- `check_type`
- `check_time`
- `wifi_ssid`
- `wifi_bssid`
- `latitude`
- `longitude`
- `distance`
- `wifi_valid`
- `location_valid`
- `success`
- `fail_reason`
- `client_info`

### att_daily_summary

考勤日汇总表。用于个人日历、月度统计和管理看板。

核心字段：

- `user_id`
- `dept_id`
- `attendance_date`
- `attendance_status`
- `work_minutes`
- `late_minutes`
- `early_minutes`
- `overtime_minutes`
- `leave_minutes`
- `is_late`
- `is_absent`
- `is_overtime`
- `is_leave`

唯一约束：

```sql
uk_summary_user_date(user_id, attendance_date)
```

## 3. 统计口径建议

个人月度统计：

- 月度工时：汇总 `att_daily_summary.work_minutes`
- 迟到次数：统计 `is_late = 1`
- 缺勤次数：统计 `is_absent = 1`
- 加班次数：统计 `is_overtime = 1`
- 加班时长：汇总 `overtime_minutes`

部门/全局统计：

- 出勤率：正常出勤人数 / 应出勤人数
- 迟到率：迟到人数 / 应出勤人数
- 缺勤率：缺勤人数 / 应出勤人数
- 请假率：请假人数 / 应出勤人数

请假数据来源：

- `app_application.application_type = 'LEAVE'`
- `app_application.status = 'APPROVED'`
- 与 `attendance_date` 交叉计算请假时长，写入 `att_daily_summary.leave_minutes`

## 4. 权限范围

管理员：

- 可创建全公司或任意部门规则
- 可查看全局和部门统计

部门主管：

- 只能创建和维护本部门规则
- 只能查看本部门统计

员工：

- 只能签到、签退
- 只能查看自己的日历和月度统计

## 5. 前端采集提醒

浏览器通常不能稳定读取 WiFi SSID/BSSID。数据库已经支持WiFi校验字段，但具体采集方式需要根据端侧能力决定：

- Web端：可先采集定位，WiFi可暂时由用户所在网络/IP策略辅助
- 移动端/客户端：可读取真实WiFi SSID/BSSID
- 后续小程序或App：再接入更可靠的WiFi能力
