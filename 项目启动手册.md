# OA 项目启动手册

`lsy` 分支同时包含 Vue 3 前端、Spring Boot 后端和数据库脚本。以下命令均从项目根目录执行，不依赖某位成员的本机路径。

## 1. 环境要求

- JDK 8 或更高版本
- Maven 3.8+
- Node.js 20+
- MySQL 8
- Redis 6+
- Elasticsearch 7.17（仅制度检索功能需要）

Nacos 默认关闭，不影响本地启动。

## 2. 初始化数据库

先创建数据库并导入基础数据：

```bash
mysql -uroot -p -e "CREATE DATABASE IF NOT EXISTS oa_system DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
mysql -uroot -p oa_system < oa_system_v2.sql
```

再按顺序执行增量脚本：

```bash
mysql -uroot -p oa_system < db_migration_20260720_standard_application_dict_doc_tag.sql
mysql -uroot -p oa_system < db_migration_20260720_attendance_schema.sql
mysql -uroot -p oa_system < db_migration_20260720_attendance_permissions_menu.sql
mysql -uroot -p oa_system < db_migration_20260720_attendance_multi_rule.sql
mysql -uroot -p oa_system < db_migration_20260720_chat_module.sql
mysql -uroot -p oa_system < db_migration_20260720_colleague_search.sql
mysql -uroot -p oa_system < db_flowable_6_8_0_init.sql
mysql -uroot -p oa_system < db_migration_20260720_flowable_approval.sql
mysql -uroot -p oa_system < db_migration_20260721_makeup_scope.sql
```

Flowable 初始化脚本只需要对空的工作流环境执行一次。若本机已经执行过，请跳过 `db_flowable_6_8_0_init.sql`。

## 3. 启动基础服务

启动 MySQL 和 Redis。需要使用制度检索时，再启动 Elasticsearch 7.17，默认地址为 `http://localhost:9200`。

## 4. 启动后端

数据库默认账号为 `root`，默认密码为 `123456`。密码不同时，通过环境变量传入。

macOS/Linux：

```bash
cd oa-attendance-system
SPRING_DATASOURCE_PASSWORD=你的MySQL密码 \
JWT_SECRET=请设置一个足够长的本地密钥 \
mvn spring-boot:run
```

Windows PowerShell：

```powershell
cd oa-attendance-system
$env:SPRING_DATASOURCE_PASSWORD = '你的MySQL密码'
$env:JWT_SECRET = '请设置一个足够长的本地密钥'
mvn spring-boot:run
```

后端地址为 `http://localhost:8080/api`。

## 5. 启动前端

另开一个终端：

```bash
cd oa-attendance-web
npm install
npm run dev
```

浏览器访问 `http://localhost:5173`。前端已配置代理，请确保后端运行在 `8080` 端口。

## 6. 测试账号

- 管理员：`admin / 123456`
- 部门主管：`manager / 123456`
- 员工：`employee / 123456`

系统不开放自行注册，员工账号由管理员创建。

## 7. 提交前检查

```bash
cd oa-attendance-system
mvn test

cd ../oa-attendance-web
npm run build
```

依赖目录、构建产物、日志和本机环境文件已通过 `.gitignore` 排除，不应提交到仓库。
