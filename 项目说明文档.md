# OA 办公系统项目说明文档

> 本文档面向新加入项目的开发人员，按功能模块列出对应的后端文件、前端文件及接口，帮助快速定位代码。

---

## 一、项目结构总览

```
OA_V1.0/
├── oa-attendance-system/          # Spring Boot 后端（端口 8080）
│   ├── src/main/java/com/oa/attendance/
│   │   ├── controller/            # 15 个控制器
│   │   ├── service/               # 服务接口
│   │   ├── service/impl/          # 服务实现
│   │   ├── entity/                # 实体类
│   │   ├── dto/                   # 数据传输对象
│   │   ├── vo/                    # 视图对象
│   │   ├── mapper/                # MyBatis-Plus Mapper
│   │   ├── config/                # 配置类
│   │   ├── util/                  # 工具类（JWT、过滤器）
│   │   ├── websocket/             # WebSocket 处理
│   │   └── exception/             # 异常处理
│   ├── src/main/resources/
│   │   ├── application.yml        # 本地配置
│   │   └── processes/             # Flowable BPMN 流程定义
│   └── nacos/                     # Nacos 配置文件
├── oa-attendance-web/             # Vue 3 前端（端口 5173）
│   └── src/
│       ├── App.vue                # 主应用（单页面，~1745 行）
│       ├── main.js                # 入口
│       ├── api/oa.js              # 所有 API 请求函数
│       ├── api/request.js         # Axios 实例（拦截器）
│       ├── style.css              # 全局样式
│       └── components/            # 9 个 Vue 组件
└── database/flowable/             # Flowable 初始化 SQL
```

---

## 二、功能模块与文件对照

### 2.1 用户认证（登录/登出/获取用户信息）

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/AuthController.java` |
| Service | `service/impl/UserServiceImpl.java`（login 逻辑） |
| Entity | `entity/SysUser.java` |
| Mapper | `mapper/SysUserMapper.java` |
| VO | `vo/LoginVO.java`、`vo/UserInfoVO.java` |
| Config | `config/SecurityConfig.java` |
| Util | `util/JwtUtil.java`、`util/JwtAuthenticationFilter.java`、`util/JwtAuthenticationEntryPoint.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（登录表单区域） |
| API | `api/oa.js`（`login`、`getUserInfo`、`logout`） |
| 请求 | `api/request.js`（Token 拦截器） |

**接口：**

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录，返回 JWT token |
| GET | `/api/auth/info` | 获取当前用户信息（含权限、菜单） |
| POST | `/api/auth/logout` | 退出登录，token 加入 Redis 黑名单 |

---

### 2.2 员工管理

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/UserController.java` |
| Service | `service/IUserService.java` |
| Service Impl | `service/impl/UserServiceImpl.java` |
| Entity | `entity/SysUser.java` |
| Mapper | `mapper/SysUserMapper.java` |
| DTO | `dto/UserCreateDTO.java`、`dto/UserUpdateDTO.java`、`dto/PasswordChangeDTO.java` |
| VO | `vo/UserVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（员工管理 section） |
| API | `api/oa.js`（`getUsers`、`createUser`、`updateUser`、`deleteUser`、`getUserById` 等） |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/users` | `user:add` | 创建员工 |
| PUT | `/api/users` | `user:update` | 更新员工信息 |
| PUT | `/api/users/profile` | 认证即可 | 修改个人资料 |
| DELETE | `/api/users/{id}` | `user:delete` | 软删除员工 |
| GET | `/api/users/{id}` | `user:query` | 根据 ID 查询员工 |
| GET | `/api/users` | `user:query` | 查询全部员工列表 |
| GET | `/api/users/dept/{deptId}` | `user:query` | 按部门查询员工 |
| PUT | `/api/users/{userId}/password` | 认证即可 | 修改个人密码 |
| PUT | `/api/users/{userId}/reset-password` | `user:update` | 管理员重置密码 |

---

### 2.3 部门管理

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/DepartmentController.java` |
| Service | `service/IDepartmentService.java` |
| Service Impl | `service/impl/DepartmentServiceImpl.java` |
| Entity | `entity/SysDepartment.java` |
| Mapper | `mapper/SysDepartmentMapper.java` |
| DTO | `dto/DepartmentDTO.java` |
| VO | `vo/DepartmentVO.java`、`vo/DepartmentTreeVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（部门管理 section，树形+列表视图） |
| API | `api/oa.js`（`getDepartments`、`createDepartment`、`updateDepartment`、`deleteDepartment`、`getDepartmentTree` 等） |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/departments` | `department:add` | 创建部门 |
| PUT | `/api/departments` | `department:update` | 更新部门 |
| DELETE | `/api/departments/{id}` | `department:delete` | 删除部门 |
| GET | `/api/departments/{id}` | `department:query` | 查询单个部门 |
| GET | `/api/departments` | `department:query` | 查询全部部门列表 |
| GET | `/api/departments/tree` | `department:query` | 获取部门树结构 |
| GET | `/api/departments/children/{parentId}` | `department:query` | 获取子部门 |

---

### 2.4 职位管理

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/PositionController.java` |
| Service | `service/IPositionService.java` |
| Service Impl | `service/impl/PositionServiceImpl.java` |
| Entity | `entity/SysPosition.java` |
| Mapper | `mapper/SysPositionMapper.java` |
| DTO | `dto/PositionDTO.java` |
| VO | `vo/PositionVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（职位管理 section） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/positions` | `position:add` | 创建职位 |
| PUT | `/api/positions` | `position:update` | 更新职位 |
| DELETE | `/api/positions/{id}` | `position:delete` | 删除职位 |
| GET | `/api/positions/{id}` | `position:query` | 查询单个职位 |
| GET | `/api/positions` | `position:query` | 查询全部职位列表 |
| GET | `/api/positions/dept/{deptId}` | `position:query` | 按部门查询职位 |

---

### 2.5 角色与权限管理

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/RoleController.java` |
| Service | `service/IRoleService.java` |
| Service Impl | `service/impl/RoleServiceImpl.java` |
| Entity | `entity/SysRole.java`、`entity/SysPermission.java` |
| Mapper | `mapper/SysRoleMapper.java`、`mapper/SysPermissionMapper.java` |
| DTO | `dto/RoleDTO.java`、`dto/RoleMenuDTO.java`、`dto/RolePermissionDTO.java` |
| VO | `vo/RoleVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 组件 | `components/AccessManagement.vue`（角色 CRUD + 菜单分配 + 权限分配） |
| 页面 | `App.vue`（权限管理 section，引用 AccessManagement） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/roles/permissions` | `role:query` | 获取系统全部权限列表 |
| POST | `/api/roles` | `role:add` | 创建角色 |
| PUT | `/api/roles` | `role:update` | 更新角色 |
| DELETE | `/api/roles/{id}` | `role:delete` | 删除角色 |
| GET | `/api/roles/{id}` | `role:query` | 查询单个角色 |
| GET | `/api/roles` | `role:query` | 查询全部角色列表 |
| PUT | `/api/roles/menus` | `role:menu` | 为角色分配菜单 |
| PUT | `/api/roles/permissions` | `role:permission` | 为角色分配权限 |

---

### 2.6 菜单管理

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/MenuController.java` |
| Service | `service/IMenuService.java` |
| Service Impl | `service/impl/MenuServiceImpl.java` |
| Entity | `entity/SysMenu.java` |
| Mapper | `mapper/SysMenuMapper.java` |
| DTO | `dto/MenuDTO.java` |
| VO | `vo/MenuVO.java`、`vo/MenuTreeVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 组件 | `components/AccessManagement.vue`（菜单树 CRUD） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/menus` | `menu:add` | 创建菜单 |
| PUT | `/api/menus` | `menu:update` | 更新菜单 |
| DELETE | `/api/menus/{id}` | `menu:delete` | 删除菜单 |
| GET | `/api/menus/{id}` | `menu:query` | 查询单个菜单 |
| GET | `/api/menus` | `menu:query` | 查询全部菜单列表 |
| GET | `/api/menus/tree` | `menu:query` | 获取菜单树 |
| GET | `/api/menus/current/tree` | 认证即可 | 获取当前用户的菜单树（侧边栏渲染） |
| GET | `/api/menus/children/{parentId}` | `menu:query` | 获取子菜单 |
| GET | `/api/menus/user/{userId}` | `menu:query` | 按用户查询菜单 |
| GET | `/api/menus/role/{roleId}` | `menu:query` | 按角色查询菜单 |

---

### 2.7 考勤管理

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/AttendanceController.java` |
| Service | `service/IAttendanceService.java` |
| Service Impl | `service/impl/AttendanceServiceImpl.java` |
| Entity | `entity/AttRule.java`、`entity/AttRuleWifi.java`、`entity/AttRuleLocation.java`、`entity/AttRecord.java`、`entity/AttCheckLog.java`、`entity/AttDailySummary.java` |
| Mapper | `mapper/AttRuleMapper.java`、`mapper/AttRuleWifiMapper.java`、`mapper/AttRuleLocationMapper.java`、`mapper/AttRecordMapper.java`、`mapper/AttCheckLogMapper.java`、`mapper/AttDailySummaryMapper.java` |
| DTO | `dto/AttendanceRuleDTO.java`、`dto/CheckInDTO.java`、`dto/CheckOutDTO.java`、`dto/AttendanceRecordModifyDTO.java` |
| VO | `vo/AttendanceRecordVO.java`、`vo/AttendanceRuleVO.java`、`vo/AttendanceStatsVO.java`、`vo/MonthlyAttendanceVO.java`、`vo/TodayAttendanceVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（考勤管理 section：打卡卡片、月度表格） |
| 组件 | `components/CalendarView.vue`（日历视图） |
| 组件 | `components/AttendanceRuleManagement.vue`（考勤规则 CRUD） |
| 组件 | `components/AttendanceRecordManagement.vue`（考勤记录修改） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| **考勤规则** | | | |
| POST | `/api/attendance/rules` | `attendance:rule:add` | 创建考勤规则 |
| PUT | `/api/attendance/rules` | `attendance:rule:update` | 更新考勤规则 |
| DELETE | `/api/attendance/rules/{ruleId}` | `attendance:rule:delete` | 删除考勤规则 |
| GET | `/api/attendance/rules/{ruleId}` | `attendance:rule:query` | 查询单个规则 |
| GET | `/api/attendance/rules` | `attendance:rule:query` | 查询规则列表 |
| GET | `/api/attendance/current-rule` | `attendance:self` | 获取当前用户适用规则 |
| GET | `/api/attendance/available-rules` | `attendance:self` | 获取用户可用规则列表 |
| **打卡操作** | | | |
| POST | `/api/attendance/check-in` | `attendance:check` | 签到 |
| POST | `/api/attendance/check-out` | `attendance:check` | 签退 |
| **考勤记录查询** | | | |
| GET | `/api/attendance/today` | `attendance:self` | 查询今日考勤记录 |
| GET | `/api/attendance/today-records` | `attendance:self` | 查询今日全部记录（多规则） |
| GET | `/api/attendance/my-month` | `attendance:self` | 查询个人月度考勤 |
| GET | `/api/attendance/user-month` | `attendance:modify` | 查询指定用户月度考勤 |
| **统计与修改** | | | |
| GET | `/api/attendance/stats` | `attendance:stats` | 考勤统计 |
| GET | `/api/attendance/stats/detail` | `attendance:stats` | 员工考勤统计详情 |
| PUT | `/api/attendance/records/modify` | `attendance:modify` | 管理员修改考勤记录 |
| POST | `/api/attendance/sync-application/{applicationId}` | `attendance:modify` | 同步审批结果到考勤 |

---

### 2.8 审批管理

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/ApplicationController.java` |
| Service | `service/IApplicationService.java` |
| Service Impl | `service/impl/ApplicationServiceImpl.java` |
| Service | `service/IAttendanceApprovalSyncService.java` |
| Service Impl | `service/impl/AttendanceApprovalSyncServiceImpl.java` |
| Service Impl | `service/impl/ApprovalReminderScheduler.java`（邮件提醒定时任务） |
| Entity | `entity/AppApplication.java` |
| Mapper | `mapper/AppApplicationMapper.java` |
| DTO | `dto/ApplicationSubmitDTO.java`、`dto/ApprovalDTO.java` |
| VO | `vo/ApplicationVO.java`、`vo/ApplicationDetailVO.java`、`vo/ApprovalTaskVO.java` |
| BPMN | `resources/processes/application-approval.bpmn20.xml` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（审批 section：发起申请、我的申请、待审批、已处理、全部申请） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| **提交申请** | | | |
| POST | `/api/applications` | `approval:submit` | 提交申请（请假/加班/补卡） |
| GET | `/api/applications/types` | `approval:self` | 获取申请类型列表 |
| GET | `/api/applications/makeup-options` | `approval:self` | 获取可补卡的异常考勤记录 |
| **查询申请** | | | |
| GET | `/api/applications/mine` | `approval:self` | 我的申请列表 |
| GET | `/api/applications/{applicationId}` | 多种权限 | 申请详情（含审批历史） |
| GET | `/api/applications/all` | `approval:all` | 全部申请（管理员） |
| **审批处理** | | | |
| GET | `/api/applications/tasks/pending` | `approval:handle` | 待审批任务列表 |
| GET | `/api/applications/tasks/handled` | `approval:handle` | 已处理任务列表 |
| POST | `/api/applications/tasks/{taskId}/approve` | `approval:handle` | 通过审批 |
| POST | `/api/applications/tasks/{taskId}/reject` | `approval:handle` | 驳回审批 |
| **撤回** | | | |
| POST | `/api/applications/{applicationId}/cancel` | `approval:self` | 撤回申请 |

---

### 2.9 即时通讯（聊天）

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/ChatController.java`（单聊） |
| Controller | `controller/GroupChatController.java`（群聊） |
| Service | `service/IChatService.java` |
| Service Impl | `service/impl/ChatServiceImpl.java` |
| Entity | `entity/ChatMessage.java`、`entity/ChatGroup.java`、`entity/ChatGroupMember.java` |
| Mapper | `mapper/ChatMessageMapper.java`、`mapper/ChatGroupMapper.java`、`mapper/ChatGroupMemberMapper.java` |
| DTO | `dto/ChatMessageDTO.java`、`dto/GroupChatCreateDTO.java` |
| VO | `vo/ChatContactVO.java`、`vo/ChatMessageVO.java`、`vo/ChatGroupVO.java` |
| WebSocket | `websocket/ChatWebSocketHandler.java` |
| WebSocket | `websocket/WebSocketAuthInterceptor.java` |
| Config | `config/WebSocketConfig.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（消息 section：联系人列表、聊天窗口、群聊、WebSocket 连接） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| **单聊** | | | |
| GET | `/api/chat/contacts` | `chat:query` | 获取联系人列表（含未读数、最后消息） |
| GET | `/api/chat/messages/{contactId}` | `chat:query` | 获取与某联系人的消息记录 |
| POST | `/api/chat/send` | `chat:query` | 发送消息 |
| **群聊** | | | |
| POST | `/api/group-chat/create` | `chat:query` | 创建群聊 |
| GET | `/api/group-chat/groups` | `chat:query` | 我的群聊列表 |
| GET | `/api/group-chat/{groupId}/messages` | `chat:query` | 获取群消息 |
| POST | `/api/group-chat/{groupId}/send` | `chat:query` | 发送群消息 |
| GET | `/api/group-chat/{groupId}/members` | `chat:query` | 获取群成员 |
| POST | `/api/group-chat/{groupId}/add-members` | `chat:query` | 添加群成员 |
| POST | `/api/group-chat/{groupId}/leave` | `chat:query` | 退出群聊 |

**WebSocket 连接：**

| 端点 | 说明 |
|------|------|
| `ws://localhost:8080/ws/chat` | 实时消息推送，需携带 JWT token 进行认证 |

---

### 2.10 通讯录

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/ColleagueController.java` |
| Service | `service/IColleagueService.java` |
| Service Impl | `service/impl/ColleagueServiceImpl.java` |
| VO | `vo/ColleagueVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 页面 | `App.vue`（通讯录 section） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/colleagues/search?keyword=` | `colleague:query` | 按姓名/工号/手机号搜索同事 |

---

### 2.11 制度文档检索

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/DocumentController.java` |
| Service | `service/IDocumentService.java` |
| Service Impl | `service/impl/DocumentServiceImpl.java` |
| Entity | `entity/DocDocument.java` |
| Mapper | `mapper/DocDocumentMapper.java` |
| Repository | `repository/PolicyDocumentRepository.java`（ES Spring Data） |
| DTO | `dto/DocumentDTO.java` |
| VO | `vo/DocumentVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 组件 | `components/DocumentCenter.vue`（文档列表、搜索、CRUD） |
| 页面 | `App.vue`（文档 section，引用 DocumentCenter） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/documents` | `document:add` | 创建文档（同步到 ES） |
| PUT | `/api/documents` | `document:update` | 更新文档 |
| DELETE | `/api/documents/{id}` | `document:delete` | 删除文档 |
| GET | `/api/documents/{id}` | `document:query` | 查询单个文档 |
| GET | `/api/documents` | `document:query` | 查询全部文档列表 |
| GET | `/api/documents/search?keyword=` | `document:query` | 全文检索（ES，高亮） |
| POST | `/api/documents/index/rebuild` | `document:index` | 重建 ES 索引 |

---

### 2.12 数据报表

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/AttendanceController.java`（stats 相关接口） |
| Service | `service/IAttendanceService.java` |
| Service Impl | `service/impl/AttendanceServiceImpl.java` |
| VO | `vo/AttendanceStatsVO.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 组件 | `components/AttendanceStats.vue`（统计指标、明细表格、导出） |
| 页面 | `App.vue`（数据报表 section，引用 AttendanceStats） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/attendance/stats` | `attendance:stats` | 考勤统计（出勤率、迟到率等） |
| GET | `/api/attendance/stats/detail` | `attendance:stats` | 员工考勤统计详情 |

---

### 2.13 文件上传

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/FileController.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| API | `api/oa.js`（`uploadFile`） |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/files/upload` | 认证即可 | 上传文件（最大 20MB），存储到 `uploads/` 目录 |

---

### 2.14 AI 智能助手

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/AgentController.java` |
| Service | `service/IAgentService.java` |
| Service Impl | `service/impl/AgentServiceImpl.java` |

**前端文件：**

| 层级 | 文件路径 |
|------|---------|
| 组件 | `components/AiAssistant.vue`（浮动聊天窗口） |
| 页面 | `App.vue`（引用 AiAssistant） |
| API | `api/oa.js` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/agent/chat` | 认证即可 | 发送自然语言指令，AI 解析并执行 |

---

### 2.15 审计日志与邮件提醒

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Controller | `controller/AuditController.java` |
| Service | `service/IAuditLogService.java` |
| Service Impl | `service/impl/AuditLogServiceImpl.java` |
| Service Impl | `service/impl/ApprovalReminderScheduler.java`（定时邮件提醒） |
| Entity | `entity/SysAuditLog.java` |
| Mapper | `mapper/SysAuditLogMapper.java` |

**接口：**

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/audit/recent` | `approval:all` | 获取最近审计日志（50 条） |
| GET | `/api/audit/application/{applicationId}` | `approval:all` | 获取指定申请的审计日志 |
| POST | `/api/audit/trigger-email-reminder` | `approval:all` | 手动触发邮件提醒 |
| GET | `/api/audit/smtp-test` | `approval:all` | 测试 SMTP 邮件发送 |

---

### 2.16 数据权限控制

**后端文件：**

| 层级 | 文件路径 |
|------|---------|
| Service | `service/DataScopeService.java` |

**说明：** 该服务用于实现不同角色的数据可见范围控制——员工只看自己数据，主管看本部门及下级部门数据，管理员看全部数据。在 `UserServiceImpl`、`AttendanceServiceImpl`、`DepartmentServiceImpl` 等多个 Service 中被调用。

---

## 三、核心中间件与配置

| 中间件 | 端口 | 用途 | 配置文件位置 |
|--------|------|------|-------------|
| MySQL | 3306 | 主数据库 `oa_system` | `nacos/attendance-system.yaml`（数据源配置） |
| Redis | 6379 | Token 黑名单、缓存 | `nacos/attendance-system.yaml` |
| Elasticsearch | 9200 | 文档全文检索 | `nacos/attendance-system.yaml` |
| Nacos | 8848 | 服务注册/发现、配置中心 | `application.yml`（当前 config 开关关闭） |
| QQ SMTP | 465 | 审批邮件提醒 | `nacos/attendance-system.yaml`（mail 配置） |
| Flowable | — | 审批工作流引擎（30 张 ACT_* 表） | `nacos/attendance-system.yaml` + `processes/application-approval.bpmn20.xml` |

---

## 四、前端组件与页面功能对照

| 组件文件 | 对应功能 | 引用位置 |
|---------|---------|---------|
| `App.vue` | 主应用壳：登录、侧边栏导航、工作台、考勤打卡、审批、员工/部门/职位管理、消息聊天、通讯录 | 入口组件 |
| `CalendarView.vue` | 考勤日历视图（月历、状态色标、日详情） | `App.vue` 日历 section |
| `AttendanceStats.vue` | 数据报表（统计指标、明细表、Excel 导出） | `App.vue` 数据报表 section |
| `AttendanceRuleManagement.vue` | 考勤规则 CRUD（工作时间、WiFi/定位验证） | `App.vue` 考勤规则 section |
| `AttendanceRecordManagement.vue` | 管理员修改考勤记录 | `App.vue` 考勤修改 section |
| `DocumentCenter.vue` | 制度文档 CRUD + ES 全文检索 | `App.vue` 文档 section |
| `AccessManagement.vue` | 角色管理 + 菜单管理 + 权限分配 | `App.vue` 权限管理 section |
| `AiAssistant.vue` | AI 智能助手浮动窗口 | `App.vue`（全局引用） |
| `HelloWorld.vue` | Vite 脚手架默认组件（未使用） | — |

---

## 五、API 接口汇总（按权限分类）

### 公开接口（无需认证）
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 用户登录 |

### 认证即可访问
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/auth/info` | 获取当前用户信息 |
| POST | `/api/auth/logout` | 退出登录 |
| PUT | `/api/users/profile` | 修改个人资料 |
| PUT | `/api/users/{userId}/password` | 修改个人密码 |
| GET | `/api/menus/current/tree` | 获取当前用户菜单树 |
| POST | `/api/files/upload` | 上传文件 |
| POST | `/api/agent/chat` | AI 助手对话 |

### 按权限码分类
| 权限码 | 对应接口 |
|--------|---------|
| `user:add` | `POST /api/users` |
| `user:update` | `PUT /api/users`、`PUT /api/users/{userId}/reset-password` |
| `user:delete` | `DELETE /api/users/{id}` |
| `user:query` | `GET /api/users`、`GET /api/users/{id}`、`GET /api/users/dept/{deptId}` |
| `department:add` | `POST /api/departments` |
| `department:update` | `PUT /api/departments` |
| `department:delete` | `DELETE /api/departments/{id}` |
| `department:query` | `GET /api/departments`、`GET /api/departments/{id}`、`GET /api/departments/tree`、`GET /api/departments/children/{parentId}` |
| `position:add` | `POST /api/positions` |
| `position:update` | `PUT /api/positions` |
| `position:delete` | `DELETE /api/positions/{id}` |
| `position:query` | `GET /api/positions`、`GET /api/positions/{id}`、`GET /api/positions/dept/{deptId}` |
| `role:add` | `POST /api/roles` |
| `role:update` | `PUT /api/roles` |
| `role:delete` | `DELETE /api/roles/{id}` |
| `role:query` | `GET /api/roles`、`GET /api/roles/{id}`、`GET /api/roles/permissions` |
| `role:menu` | `PUT /api/roles/menus` |
| `role:permission` | `PUT /api/roles/permissions` |
| `menu:add` | `POST /api/menus` |
| `menu:update` | `PUT /api/menus` |
| `menu:delete` | `DELETE /api/menus/{id}` |
| `menu:query` | `GET /api/menus`、`GET /api/menus/{id}`、`GET /api/menus/tree`、`GET /api/menus/children/{parentId}`、`GET /api/menus/user/{userId}`、`GET /api/menus/role/{roleId}` |
| `attendance:rule:add` | `POST /api/attendance/rules` |
| `attendance:rule:update` | `PUT /api/attendance/rules` |
| `attendance:rule:delete` | `DELETE /api/attendance/rules/{ruleId}` |
| `attendance:rule:query` | `GET /api/attendance/rules`、`GET /api/attendance/rules/{ruleId}` |
| `attendance:check` | `POST /api/attendance/check-in`、`POST /api/attendance/check-out` |
| `attendance:self` | `GET /api/attendance/today`、`GET /api/attendance/today-records`、`GET /api/attendance/my-month`、`GET /api/attendance/current-rule`、`GET /api/attendance/available-rules` |
| `attendance:stats` | `GET /api/attendance/stats`、`GET /api/attendance/stats/detail` |
| `attendance:modify` | `PUT /api/attendance/records/modify`、`POST /api/attendance/sync-application/{applicationId}`、`GET /api/attendance/user-month` |
| `approval:submit` | `POST /api/applications` |
| `approval:self` | `GET /api/applications/mine`、`GET /api/applications/types`、`GET /api/applications/makeup-options`、`POST /api/applications/{applicationId}/cancel` |
| `approval:handle` | `GET /api/applications/tasks/pending`、`GET /api/applications/tasks/handled`、`POST /api/applications/tasks/{taskId}/approve`、`POST /api/applications/tasks/{taskId}/reject` |
| `approval:all` | `GET /api/applications/all`、`GET /api/audit/recent`、`GET /api/audit/application/{applicationId}`、`POST /api/audit/trigger-email-reminder`、`GET /api/audit/smtp-test` |
| `chat:query` | 全部 `/api/chat/**` 和 `/api/group-chat/**` 接口 |
| `document:add` | `POST /api/documents` |
| `document:update` | `PUT /api/documents` |
| `document:delete` | `DELETE /api/documents/{id}` |
| `document:query` | `GET /api/documents`、`GET /api/documents/{id}`、`GET /api/documents/search` |
| `document:index` | `POST /api/documents/index/rebuild` |
| `colleague:query` | `GET /api/colleagues/search` |

---

## 六、快速定位指南

| 场景 | 从哪里入手 |
|------|-----------|
| 修改登录逻辑 | `AuthController` → `UserServiceImpl` → `JwtUtil` |
| 新增考勤状态 | `AttendanceServiceImpl`（状态计算逻辑）→ `AttRecord` 实体 |
| 调整审批流程 | `application-approval.bpmn20.xml` → `ApplicationServiceImpl` → `AttendanceApprovalSyncServiceImpl` |
| 新增前端页面 | `App.vue`（添加 section）→ 新建 `components/Xxx.vue` → `api/oa.js`（添加 API 函数） |
| 新增接口权限 | `SysPermission` 表插入权限码 → 对应 Controller 方法加 `@PreAuthorize` |
| 调整 WebSocket 消息 | `ChatWebSocketHandler` → `App.vue`（WebSocket 消息处理） |
| 修改邮件模板 | `ApprovalReminderScheduler` → `nacos/attendance-system.yaml`（mail 配置） |
| 添加 ES 搜索字段 | `DocDocument` 实体 → `PolicyDocumentRepository` → `DocumentCenter.vue` |