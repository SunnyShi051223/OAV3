# OA 项目启动手册

本文用于团队成员在本地启动 OA 项目。当前统一约定：开发环境组件全部放在 `D:\DevTools` 下。

## 1. 目录约定

项目根目录：

```powershell
D:\OAV3
```

后端目录：

```powershell
D:\OAV3\oa-attendance-system
```

前端目录：

```powershell
D:\OAV3\oa-attendance-web
```

开发工具目录：

```powershell
D:\DevTools
```

已确认的本机工具路径：

```powershell
D:\DevTools\Java\jdk-21
D:\DevTools\Maven\apache-maven-3.9.16\bin\mvn.cmd
D:\DevTools\Redis
D:\DevTools\Elasticsearch\elasticsearch-7.17.0\bin\elasticsearch.bat
D:\DevTools\nacos\bin\startup.cmd
```

## 2. 启动顺序

建议按下面顺序启动：

1. MySQL
2. Redis
3. Elasticsearch
4. Nacos
5. 后端 Spring Boot
6. 前端 Vue/Vite

## 3. MySQL

当前后端连接配置：

```text
地址：localhost:3306
数据库：oa_system
用户名：root
密码：shisannian1223
```

首次初始化或需要重建数据时，在 MySQL 中导入：

```powershell
mysql -uroot -pshisannian1223 oa_system < D:\HuaweiMoveData\Users\32874\Desktop\OAV3\oa_system_v2.sql
```

确认数据库可连接：

```powershell
mysql -uroot -pshisannian1223 -e "SHOW DATABASES;"
```

## 4. Redis

默认端口：`6379`

进入 Redis 目录：

```powershell
cd /d D:\DevTools\Redis
```

启动 Redis：

```powershell
.\redis-server.exe .\redis.windows.conf
```

如果本机 Redis 配置文件不是 `redis.windows.conf`，使用实际配置文件名，例如：

```powershell
.\redis-server.exe .\redis.conf
```

验证 Redis：

```powershell
.\redis-cli.exe ping
```

正常返回：

```text
PONG
```

## 5. Elasticsearch

版本目录：

```powershell
D:\DevTools\Elasticsearch\elasticsearch-7.17.0
```

默认端口：`9200`

启动命令：

```powershell
cd /d D:\DevTools\Elasticsearch\elasticsearch-7.17.0\bin
.\elasticsearch.bat
```

验证地址：

```text
http://localhost:9200
```

PowerShell 验证：

```powershell
Invoke-RestMethod http://localhost:9200
```

## 6. Nacos

Nacos 目录：

```powershell
D:\DevTools\nacos
```

服务端口：`8848`

如果控制台端口已配置为 `8849`，访问：

```text
http://localhost:8849/nacos
```

常规启动方式：

```powershell
cd /d D:\DevTools\nacos\bin
.\startup.cmd -m standalone
```

如果遇到 Java 21 模块访问报错，可使用下面方式启动：

```powershell
D:\DevTools\Java\jdk-21\bin\java.exe `
  --add-opens=java.base/java.lang=ALL-UNNAMED `
  --add-opens=java.base/java.lang.reflect=ALL-UNNAMED `
  --add-opens=java.base/java.util=ALL-UNNAMED `
  -Xms512m -Xmx512m `
  -Dnacos.standalone=true `
  -Dnacos.home=D:\DevTools\nacos `
  -Dloader.path=D:\DevTools\nacos\plugins,D:\DevTools\nacos\plugins\health,D:\DevTools\nacos\plugins\cmdb,D:\DevTools\nacos\plugins\selector `
  -jar D:\DevTools\nacos\target\nacos-server.jar `
  --spring.config.additional-location=file:D:\DevTools\nacos\conf\ `
  --logging.config=D:\DevTools\nacos\conf\nacos-logback.xml
```

项目内提供了一份 Nacos 配置文件：

```powershell
D:\HuaweiMoveData\Users\32874\Desktop\OAV3\oa-attendance-system\nacos\attendance-system.yaml
```

当前后端 `application.yml` 中 `spring.cloud.nacos.config.enabled=false`，所以默认以后端本地配置启动；Nacos 作为技术栈组件启动即可。若团队决定改为配置中心模式，需要将该开关改为 `true`，并在 Nacos 中发布：

```text
Data ID：attendance-system.yaml
Group：DEFAULT_GROUP
```

## 7. 后端启动

后端端口：`8080`

后端路径：

```powershell
D:\HuaweiMoveData\Users\32874\Desktop\OAV3\oa-attendance-system
```

进入后端目录：

```powershell
cd /d D:\HuaweiMoveData\Users\32874\Desktop\OAV3\oa-attendance-system
```

推荐使用固定 Maven 路径启动：

```powershell
D:\DevTools\Maven\apache-maven-3.9.16\bin\mvn.cmd spring-boot:run
```

如果已配置 Maven 到系统 PATH，也可以使用：

```powershell
mvn spring-boot:run
```

后端接口地址：

```text
http://localhost:8080/api
```

## 8. 前端启动

前端端口：`5173`

前端路径：

```powershell
D:\HuaweiMoveData\Users\32874\Desktop\OAV3\oa-attendance-web
```

进入前端目录：

```powershell
cd /d D:\HuaweiMoveData\Users\32874\Desktop\OAV3\oa-attendance-web
```

首次拉取项目或依赖变化后安装依赖：

```powershell
npm install
```

启动前端：

```powershell
npm run dev
```

访问地址：

```text
http://localhost:5173
```

当前前端接口代理/请求地址写在：

```powershell
D:\HuaweiMoveData\Users\32874\Desktop\OAV3\oa-attendance-web\src\api\http.js
```

当前后端地址为：

```text
http://localhost:8080/api
```

## 9. 常用账号

```text
管理员：admin / 123456
部门主管：manager / 123456
员工：employee / 123456
```

## 10. 常用检查命令

检查 Redis：

```powershell
cd /d D:\DevTools\Redis
.\redis-cli.exe ping
```

检查 Elasticsearch：

```powershell
Invoke-RestMethod http://localhost:9200
```

检查前端页面：

```text
http://localhost:5173
```

检查后端登录接口：

```powershell
Invoke-RestMethod -Method Post `
  -Uri "http://localhost:8080/api/auth/login" `
  -ContentType "application/json" `
  -Body '{"username":"admin","password":"123456"}'
```

## 11. 常见问题

端口被占用：

```powershell
netstat -ano | findstr :8080
netstat -ano | findstr :5173
netstat -ano | findstr :6379
netstat -ano | findstr :9200
netstat -ano | findstr :8848
```

前端白屏或接口无数据：

1. 确认后端 `8080` 已启动。
2. 确认浏览器已重新登录，旧 token 可能带旧权限。
3. 确认 Redis 正常，登录和退出会用到 token 黑名单。
4. 确认数据库已导入最新 `oa_system_v2.sql`。

制度检索无结果：

1. 确认 Elasticsearch `9200` 已启动。
2. 使用管理员账号进入制度检索页面。
3. 点击“重建索引”后再检索。

Maven 下载依赖慢或失败：

1. 确认网络正常。
2. 确认 Maven 使用 `D:\DevTools\Maven\apache-maven-3.9.16\bin\mvn.cmd`。
3. 如团队配置了内网镜像，同步 `settings.xml` 到本机 Maven 配置目录。
